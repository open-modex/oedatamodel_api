{
  "base_mapping": "concrete",
  "mapping": {
    "oed_scalars": "map(&set(@, 'region', join(',', @.region)), oed_scalars)",
    "oed_scenario": "oed_scenario",
    "oed_timeseries": "map(&set(@, 'region', join(',', @.region)), oed_timeseries)"
  }
}
