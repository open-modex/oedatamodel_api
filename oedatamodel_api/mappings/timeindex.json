
{
  "base_mapping": "grouped",
  "mapping": {
    "series": "map(&merge_array(map(&to_object([[join('-', [@.region, @.parameter_name]), @.series]]), @)), timeseries.*)",
    "timeindex": "to_object(zip(keys(timeseries), timeseries.*[0] | [].series))"
  }
}