{
  "base_mapping": "concrete",
  "mapping": {
    "regions": "map(&to_object([@]), zip(repeat('region', length(unique(oed_scalars[*].region[]))), unique(oed_scalars[*].region[])))",
    "scenario": "oed_scenario"
  }
}
